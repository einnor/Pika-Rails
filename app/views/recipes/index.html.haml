#recipes.transitions-enabled

  - @recipes.each do |recipe|
    .box.panel.panel-default
      = link_to (image_tag recipe.cover.url), recipe
      .panel-body
        %h2= link_to recipe.name, recipe
        %div.btn-group
          = link_to 'View', recipe, class: 'btn btn-default'
          - if user_signed_in?
            = link_to 'Edit', edit_recipe_path(recipe), class: 'btn btn-default'
            = link_to 'Delete', recipe_path(recipe), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default'
        %p.user
          Submitted by
          = recipe.user.email
